<Window x:Class="SteamServerTool.Dialogs.AddRemoteServerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Remote Server"
        Height="310" Width="480"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource DarkBgBrush}"
        Foreground="{StaticResource ForegroundBrush}"
        FontFamily="Segoe UI" FontSize="13">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="🌐" FontSize="22" VerticalAlignment="Center" Margin="0,0,10,0"
                       Foreground="{StaticResource AccentBrush}"/>
            <StackPanel>
                <TextBlock Text="Add Remote Server" FontSize="16" FontWeight="SemiBold"/>
                <TextBlock Text="Connect via RCON for monitoring and administration."
                           Foreground="{StaticResource DimForegroundBrush}" FontSize="11"/>
            </StackPanel>
        </StackPanel>

        <!-- Form -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>
                <RowDefinition Height="36"/>
                <RowDefinition Height="36"/>
                <RowDefinition Height="36"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Display Name:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="0" Grid.Column="1" x:Name="TxtName"
                     Margin="0,2" VerticalAlignment="Center"
                     Tag="e.g. My Remote ARK"/>

            <Label Grid.Row="1" Grid.Column="0" Content="RCON Host:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="1" Grid.Column="1" x:Name="TxtHost"
                     Margin="0,2" VerticalAlignment="Center"
                     Tag="e.g. 192.168.1.100"/>

            <Label Grid.Row="2" Grid.Column="0" Content="RCON Port:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="2" Grid.Column="1" x:Name="TxtPort"
                     Margin="0,2" VerticalAlignment="Center"
                     Text="27020"/>

            <Label Grid.Row="3" Grid.Column="0" Content="RCON Password:" VerticalAlignment="Center"/>
            <PasswordBox Grid.Row="3" Grid.Column="1" x:Name="TxtPassword"
                         Margin="0,2" VerticalAlignment="Center"/>
        </Grid>

        <!-- Buttons -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="2"
                    Content="Cancel"
                    Padding="18,8"
                    IsCancel="True"
                    Click="BtnCancel_Click"/>

            <Button Grid.Column="4"
                    x:Name="BtnAdd"
                    Content="＋ Add Remote Server"
                    Padding="18,8"
                    Style="{StaticResource AccentButton}"
                    Click="BtnAdd_Click"/>
        </Grid>
    </Grid>
</Window>
